################
### MONITORS ###
################

# DEFAULT CONF (example)
# monitor=,preferred,auto,auto

monitor = DP-2, 1920x1200@60, -1200x-420, 1, transform, 3 # ASUS - left/rotated
monitor = DP-3, 1920x1080@180, 0x0, 1 # AOC - center
monitor = DP-1, 1440x900@75, 1920x0, 1 # Samsung - right

## Alt config for my laptop
#monitor = HDMI-A-2, 1920x1080@60, 0x0, 1
#monitor = eDP-1, 1920x1080@60, 0x0, 1 # Compatibility with my laptop lmao

# Optionally disable the inbuilt monitor if the external one is connected
#monitor = eDP-1, disable

#############################
### ENVIRONMENT VARIABLES ###
#############################

# Vars for software I use
$terminal = kitty
$fileManager = thunar
$menu = wofi --conf/home/nyx/.config/wofi/config --style /home/nyx/.config/wofi/style.css
$browser = librewolf

# QT
env = QT_QPA_PLATFORMTHEME,qt5ct
env = QT_STYLE_OVERRIDE,kvantum
env = QT_QPA_PLATFORM,wayland

# Set Cursor size
env = XCURSOR_SIZE,24
env = HYPRCURSOR_SIZE,24

# NoVideo
env = LIBVA_DRIVER_NAME,nvidia
env = XDG_SESSION_TYPE,wayland
env = GBM_BACKEND,nvidia-drm
env = __GLX_VENDOR_LIBRARY_NAME,nvidia
env = NVD_BACKEND,direct

# GTK
env = GDK_BACKEND,wayland

# No update info
ecosystem:no_update_news = true

#################
### AUTOSTART ###
#################

# Misc autorun stuff: gamma control, top bar, wallpaper
exec-once = hyprsunset -t 4000
exec-once = hyprctl setcursor catppuccin-mocha-pink-cursors 24
exec-once = /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1 # auth agent
exec-once = waybar
exec-once = mako

# Fix slow app launch
exec-once = hash dbus-update-activation-environment 2>/dev/null &
exec-once = dbus-update-activation-environment &

# Thy apps
exec-once = [workspace 3 silent] $browser
exec-once = [workspace 2 silent] equibop
exec-once = sleep 1 && hyprctl dispatch workspace 2

#################
### APPERANCE ###
#################

# Cursor
cursor {
    no_hardware_cursors = true
}

# Set smaller borders, gaps, enable tearing
general {
    gaps_in = 2
    gaps_out = 3

    border_size = 1

    col.active_border = rgba(cba6f7FF)
    col.inactive_border = rgba(45475aFF)

    # Me no like that tbh
    resize_on_border = false

    allow_tearing = true

    layout = dwindle
}

# Set smaller rounding, shadows, opacity, blur
decoration {
    rounding = 2

    active_opacity = 1.0
    inactive_opacity = 1.0

    blur {
        enabled = false
    }
}

dwindle {
    pseudotile = true # Master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = true # You probably want this
}

misc {
    force_default_wallpaper = 0 # Set to 0 or 1 to disable the anime mascot wallpapers
    disable_hyprland_logo = true # If true disables the random hyprland logo / anime girl background. :(
    vrr = 1
}

# https://wiki.hyprland.org/Configuring/Variables/#animations
animations {
    enabled = yes, please :)

    # Default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

    bezier = easeOutQuint,0.23,1,0.32,1
    bezier = easeInOutCubic,0.65,0.05,0.36,1
    bezier = linear,0,0,1,1
    bezier = almostLinear,0.5,0.5,0.75,1.0
    bezier = quick,0.15,0,0.1,1

    animation = global, 1, 10, default
    animation = border, 1, 5.39, easeOutQuint
    animation = windows, 1, 4.79, easeOutQuint
    animation = windowsIn, 1, 4.1, easeOutQuint, popin 87%
    animation = windowsOut, 1, 1.49, linear, popin 87%
    animation = fadeIn, 1, 1.73, almostLinear
    animation = fadeOut, 1, 1.46, almostLinear
    animation = fade, 1, 3.03, quick
    animation = layers, 1, 3.81, easeOutQuint
    animation = layersIn, 1, 4, easeOutQuint, fade
    animation = layersOut, 1, 1.5, linear, fade
    animation = fadeLayersIn, 1, 1.79, almostLinear
    animation = fadeLayersOut, 1, 1.39, almostLinear
    animation = workspaces, 1, 1.94, almostLinear, fade
    animation = workspacesIn, 1, 1.21, almostLinear, fade
    animation = workspacesOut, 1, 1.94, almostLinear, fade
}

#############
### INPUT ###
#############

# holy shit im polish no way
input {
    kb_layout = pl
    kb_variant =
    kb_model =
    kb_options =
    kb_rules =

    follow_mouse=1
    mouse_refocus=false

    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.

    touchpad {
        natural_scroll = false
    }
}

###################
### KEYBINDINGS ###
###################

$mainMod = SUPER # Sets "Windows" key as main modifier

# Main binds
bind = $mainMod, Q, exec, $terminal
bind = $mainMod, C, killactive,
bind = $mainMod, M, exec, wlogout
bind = $mainMod, E, exec, $fileManager
bind = $mainMod, V, togglefloating,
bind = $mainMod, R, exec, $menu
bind = $mainMod, P, pseudo, # dwindle
bind = $mainMod, F, exec, $browser

# Move focus with mainMod + arrow keys
bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Moving window focus
bind = SUPER, left, movefocus, l
bind = SUPER, right, movefocus, r
bind = SUPER, up, movefocus, u
bind = SUPER, down, movefocus, d

# Example special workspace (scratchpad)
bind = $mainMod, S, togglespecialworkspace, magic
bind = $mainMod SHIFT, S, movetoworkspace, special:magic

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# Audio and brightness
bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 2%+
bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 2%-
bindel = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bind = ,XF86MonBrightnessDown, exec, brightnessctl s 10%-
bind = ,XF86MonBrightnessUp, exec, brightnessctl s +10%
bind = ,XF86AudioPlay, exec, playerctl play-pause
bind = ,XF86AudioPause, exec, playerctl play-pause
bind = ,XF86AudioNext, exec, playerctl next
bind = ,XF86AudioPrev, exec, playerctl previous

# screenshots, lock, color picker
bind = ,Print, exec, grimblast --freeze --notify copy area
bind = $mainMod CONTROL, R, exec, grimblast --freeze --notify --cursor copy active
bind = $mainMod CONTROL, T, exec, grimblast --freeze --notify --cursor copy output
bind = $mainMod CONTROL, L, exec, swaylock -c 000000
bind = $mainMod CONTROL, X, exec, hyprpicker -a -n

##############################
### WINDOWS AND WORKSPACES ###
##############################

# If i were to ever come back to coding python guis,,,
windowrulev2 = float, title:main.py
windowrulev2 = float, title:Main Window

windowrulev2 = suppressevent maximize, class:.* # idk

